import Container from "@/layout/admin/container";
import WebinarsTable from "@/layout/admin/tables/webinar";

const Registered = () => {
  return (
    <Container active="Webinars">
    

      <WebinarsTable/>
    </Container>
  );
};

export default Registered;
